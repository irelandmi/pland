[project]
name = "pland"
version = "0.1.0"
description = "LLM-powered project planner for taskd"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.28",
]

[project.scripts]
pland = "pland.cli:main"
